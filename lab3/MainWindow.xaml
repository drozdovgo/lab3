<Window x:Class="lab3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lab3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <Slider Minimum="8" Maximum="30" ValueChanged="Slider_ValueChanged"></Slider>
            <TextBlock x:Name="Message" FontSize="{Binding ElementName=SliderFontSize, Path=Value}">
                Пример WPF-приложения для демонстрации привязки данных
            </TextBlock>
            <TextBox x:Name="t1" Text="{Binding ElementName=t1, Path=Text}"/>
            <TextBox x:Name="t2" Text="{Binding ElementName=t2, Path=Text, Mode=OneTime}"/>
            <Slider x:Name="slider1" />
            <Slider x:Name="slider2" Value="{Binding ElementName=slider1, Path=Value, Mode=OneTime}" />
            <TextBox x:Name="t3" Text="{Binding ElementName=size, Path=Value, Mode=TwoWay}"/>
            <Slider x:Name="size"  Minimum="8"  Maximum="30"  Value="0"/>
            <TextBlock x:Name="WW" Text="DOTA2 ++"  FontSize="{Binding ElementName=size, Path=Value}"/>
        </StackPanel>
        
    </Grid>
</Window>
